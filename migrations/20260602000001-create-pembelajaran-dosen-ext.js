'use strict';

module.exports = {
  async up(queryInterface, Sequelize) {
    // Empty migration (moved to 20260515000001-create-pembelajaran-dosen-ext.js to fix timestamp ordering)
  },

  async down(queryInterface, Sequelize) {
  }
};
